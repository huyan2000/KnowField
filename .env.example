# Optional: paper metadata API key for higher rate limits.
# Keep the real value only in your local .env.
S2_API_KEY=

# Optional: directory for local PDFs. The project never commits this directory.
PAPER_PDF_DIR=./PHD-Buyya
